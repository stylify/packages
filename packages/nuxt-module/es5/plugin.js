/**
 * @stylify/nuxt-module v0.0.1 
 * (c) 2020-2021 Vladimír Macháček
 * Released under the MIT License.
 */

/* import Stylify from '@stylify/stylify/dist/stylify.native';
import Profiler from '@stylify/stylify/dist/profiler.browser'; */

function plugin (ctx) {
	//const runtimeConfig = ctx.$config && ctx.$config.stylify || {};
 	/* Stylify.configure({
		compiler: compilerConfig
	});

	if (typeof window !== 'undefined') {
		Profiler.init(Stylify);
	} */
	console.log('fu');
}

export default plugin;
